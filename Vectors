A <- c(rep (1,5), rep(2,5)) 
A1 <- rep (c (1,2),each = 5)

B <- seq(from = 10, to = 1 )
B1 <- B[c(3,4,5,6)]
B [which (B<7)] 
B [which (B>=9)] 

E <- c("ATTTACCAGGAGTC", "ATTCCGAGATGCGT", "AAAACCTTGGAACG", "TAGG")
E[1:3] 
E [2] 

D <- c (2 ^ (0:7)) 
d <- c (D [8]-28)
D1 <- c (D,d)


